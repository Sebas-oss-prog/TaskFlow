<UserControl x:Class="TaskFlow.Views.MyTasksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:material="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:conv="clr-namespace:TaskFlow.Converters">

    <UserControl.Resources>
        <conv:StatusToBrushConverter x:Key="StatusConverter"/>
    </UserControl.Resources>

    <Grid Background="{DynamicResource MaterialDesign.Brush.Background}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Margin="24,20,24,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Мои задачи" FontSize="30" FontWeight="SemiBold"/>
                <TextBlock Text="Управляйте задачами, отслеживайте статус и быстро создавайте новые поручения." Margin="0,8,0,0" Opacity="0.72" FontSize="14"/>
            </StackPanel>

            <Button Grid.Column="1"
                    Content="Новая задача"
                    Height="44"
                    Padding="26,0"
                    Command="{Binding CreateTaskCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </Grid>

        <Border Grid.Row="1" Margin="24,0,24,16" Padding="18" Background="#FFF7F9FC" CornerRadius="18">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Фильтр по статусу" VerticalAlignment="Center" Margin="0,0,14,0" FontWeight="Medium"/>
                <ComboBox Width="180"
                          ItemsSource="{Binding StatusFilters}"
                          SelectedItem="{Binding SelectedStatusFilter}"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}"/>
            </StackPanel>
        </Border>

        <ListView Grid.Row="2"
                  Margin="24,0,24,24"
                  ItemsSource="{Binding FilteredTasks}"
                  BorderThickness="0"
                  Background="Transparent">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <material:Card Margin="0,0,0,12" Padding="18" UniformCornerRadius="18">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel>
                                <TextBlock Text="{Binding Title}" FontSize="17" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Description}" Margin="0,8,0,0" TextWrapping="Wrap" Opacity="0.76"/>
                                <WrapPanel Margin="0,14,0,0">
                                    <Border Padding="10,4" CornerRadius="12" Background="#FFF1F5F9">
                                        <TextBlock Text="{Binding ResponsibleName}" FontWeight="Medium" FontSize="12.5"/>
                                    </Border>
                                    <Border Margin="8,0,0,0" Padding="10,4" CornerRadius="12" Background="#FFF8F4EA">
                                        <TextBlock Text="{Binding Priority}" FontWeight="Medium" FontSize="12.5"/>
                                    </Border>
                                    <Border Margin="8,0,0,0" Padding="10,4" CornerRadius="12" Background="#FFF3F7EC">
                                        <TextBlock Text="{Binding DueDate, StringFormat=Срок: {0:dd.MM.yyyy}}" FontWeight="Medium" FontSize="12.5"/>
                                    </Border>
                                </WrapPanel>
                            </StackPanel>

                            <Button Grid.Column="1"
                                    MinWidth="120"
                                    Height="38"
                                    Margin="18,0,0,0"
                                    Padding="18,0"
                                    Command="{Binding DataContext.CycleTaskStatusCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                    CommandParameter="{Binding}"
                                    Background="{Binding Status, Converter={StaticResource StatusConverter}}"
                                    Foreground="White"
                                    FontWeight="SemiBold"
                                    BorderThickness="0"
                                    Content="{Binding Status}"/>
                        </Grid>
                    </material:Card>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>

