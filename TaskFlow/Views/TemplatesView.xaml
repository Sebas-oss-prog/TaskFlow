<UserControl x:Class="TaskFlow.Views.TemplatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:material="http://materialdesigninxaml.net/winfx/xaml/themes">

    <Grid Background="{DynamicResource MaterialDesign.Brush.Background}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="24,20,24,16">
            <TextBlock Text="Шаблоны задач" FontSize="30" FontWeight="SemiBold"/>
            <TextBlock Text="Готовые сценарии для типовых рабочих задач. Выберите карточку, откройте форму и при необходимости скорректируйте детали." Margin="0,8,0,0" Opacity="0.72" FontSize="14"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" Margin="24,0,24,24" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding TaskTemplates}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <material:Card Width="320" Margin="0,0,16,16" Padding="20" UniformCornerRadius="22">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Border Width="64" Height="8" Background="{Binding AccentColor}" CornerRadius="4" HorizontalAlignment="Left"/>

                                <StackPanel Grid.Row="1" Margin="0,18,0,0">
                                    <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding Category}" Margin="0,8,0,0" FontWeight="Medium" Opacity="0.68"/>
                                </StackPanel>

                                <TextBlock Grid.Row="2" Text="{Binding Description}" Margin="0,14,0,0" TextWrapping="Wrap" Opacity="0.8"/>

                                <Grid Grid.Row="3" Margin="0,18,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Orientation="Horizontal">
                                        <Border Padding="10,4" CornerRadius="12" Background="#FFF8F4EA">
                                            <TextBlock Text="{Binding Priority}" FontWeight="Medium" FontSize="12.5"/>
                                        </Border>
                                        <Border Margin="8,0,0,0" Padding="10,4" CornerRadius="12" Background="#FFF1F5F9">
                                            <TextBlock Text="{Binding DefaultDueInDays, StringFormat=+{0} дн.}" FontWeight="Medium" FontSize="12.5"/>
                                        </Border>
                                    </StackPanel>

                                    <Button Grid.Column="1"
                                            Content="Применить"
                                            Padding="18,0"
                                            Height="38"
                                            Command="{Binding DataContext.ApplyTemplateCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource MaterialDesignOutlinedButton}"/>
                                </Grid>
                            </Grid>
                        </material:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>

